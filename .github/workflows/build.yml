name: Build All

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install python-appimage
      - name: Build AppImage
        run: |
          python-appimage build app -p 3.11 -o KolocPlayer.AppImage v0.3.0_gui.py
      - name: Upload AppImage
        uses: actions/upload-artifact@v4
        with:
          name: KolocPlayer-AppImage
          path: KolocPlayer.AppImage

  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller
      - name: Build EXE
        run: |
          pyinstaller --onefile --windowed --name "KolocPlayer" v0.3.0_gui.py
      - name: Upload EXE
        uses: actions/upload-artifact@v4
        with:
          name: KolocPlayer-Windows
          path: dist/KolocPlayer.exe
